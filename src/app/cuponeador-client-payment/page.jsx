import { Suspense } from "react";
import CuponeadorClientPayment from "./content";

export default function CuponeadorClientPaymentPage() {
  return (
    <Suspense>
      <CuponeadorClientPayment />
    </Suspense>
  );
}
